<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Auth token store must never be backed up (API < 31 path; see data_extraction_rules.xml). -->
    <exclude domain="file" path="datastore/auth.preferences_pb" />
</full-backup-content>
